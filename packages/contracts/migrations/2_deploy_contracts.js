module.exports = async function (deployer, network) {
    console.log("NOTE FROM ED: contracts are now deployed in the initial migration file");
    console.log("this is because otherwise deploying the migrations contract ");
    console.log("on mainnet is a dependency for my actual contract to deploy");
    return;
};
